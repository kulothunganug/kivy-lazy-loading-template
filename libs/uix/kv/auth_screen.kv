<AuthScreen>

    BoxLayout:
        orientation: "vertical"

        Label:
            text: "Current Screen: " + root.name

        Button:
            text: "Login (Dummy)"
            on_release: root.manager.push_replacement("home")
