<SettingsScreen>

    BoxLayout:
        orientation: "vertical"

        Label:
            text: "Current Screen: " + root.name

        Button:
            text: "GOTO HOME"
            on_release: root.manager.pop()
